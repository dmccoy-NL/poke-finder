import { BrowserRouter } from "react-router-dom";
import Users from "./pages/Users";

const Router = () => {
  return (
    <BrowserRouter>
      <Users />
    </BrowserRouter>
  );
};

export default Router;
